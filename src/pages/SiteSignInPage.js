import React from 'react';
import SiteSignInContainer from '../containers/SiteSignInContainer';

const SiteSignInPage = props => (
  <SiteSignInContainer {...props} />
);

export default SiteSignInPage;
