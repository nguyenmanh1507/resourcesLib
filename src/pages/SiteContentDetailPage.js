import React from 'react';
import SiteContentDetailContainer from '../containers/SiteContentDetailContainer';

const SiteContentDetailPage = props => (
  <SiteContentDetailContainer {...props} />
);

export default SiteContentDetailPage;
