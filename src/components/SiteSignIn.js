import React from 'react';

const SiteSignIn = props => (
  <div className="ui center aligned sixteen wide column">
    <h2 className="">Sign In using</h2>
    <button className="ui facebook button big">
      <i className="facebook icon"></i>
      Facebook
    </button>
    <button className="ui google plus button big">
      <i className="google plus icon"></i>
      Google Plus
    </button>
  </div>
);

export default SiteSignIn;
